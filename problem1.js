words = ["flick", "codewars", "yellow", "blue"];
const flickSwitch = (array) => {
  for (let i = 0; i < array.length; i++)
    if (array[i]("flick")) {
      return true;
    } else if (array.length[i](array)) {
      return false;
    }
};
flickSwitch(words);
